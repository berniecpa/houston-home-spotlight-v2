---
phase: 05-admin-panel-agent-profiles
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/data.ts
  - src/app/agents/[slug]/page.tsx
  - src/components/AgentProfileHeader.tsx
  - src/tests/agent-profile-page.test.ts
  - src/tests/agent-profile-query.test.ts
autonomous: true
requirements: [ADMIN-04]
must_haves:
  truths:
    - "Visiting /agents/[slug] for a non-suspended agent shows their name, photo, brokerage, license number, and active publicly-visible listings"
    - "The public profile NEVER renders the agent's email or phone"
    - "/agents/[slug] returns notFound() when the slug does not resolve OR the agent is suspended"
    - "The profile page is accessible without login (force-dynamic RSC reading D1)"
  artifacts:
    - path: "src/lib/data.ts"
      provides: "getAgentProfileBySlug returning profile fields + active visible listings"
      contains: "getAgentProfileBySlug"
    - path: "src/app/agents/[slug]/page.tsx"
      provides: "Public force-dynamic agent profile RSC"
      contains: "force-dynamic"
    - path: "src/components/AgentProfileHeader.tsx"
      provides: "Profile header presentation (no PII)"
      contains: "brokerage"
  key_links:
    - from: "src/app/agents/[slug]/page.tsx"
      to: "src/lib/data.ts getAgentProfileBySlug"
      via: "await call in RSC body"
      pattern: "getAgentProfileBySlug"
    - from: "src/app/agents/[slug]/page.tsx"
      to: "src/components/ListingCard.tsx"
      via: "map over visible listings"
      pattern: "ListingCard"
---

<objective>
Build the public agent profile page at `/agents/[slug]`: a force-dynamic RSC that resolves an agent by slug, shows their public-safe identity (display_name, photo_url, brokerage, license_number) and the agent's active, publicly-visible listings via reused ListingCard, and calls notFound() when the slug is unknown or the agent is suspended. Never expose email or phone.

Purpose: Delivers ADMIN-04 — every agent has a discoverable public profile. Reuses the Plan 01 visibility gate (AGENT_VISIBLE_SQL) and the generated slug so the page composes cleanly with suspension and subscription rules.

Output: getAgentProfileBySlug in data.ts (profile fields + visible listings); the page RSC; an AgentProfileHeader presentation component; two source-grep test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel-agent-profiles/05-CONTEXT.md
@.planning/phases/05-admin-panel-agent-profiles/05-01-PLAN.md

@src/lib/data.ts
@src/lib/subscription.ts
@src/components/ListingCard.tsx
@src/app/listings/[slug]/page.tsx
@db/migrations/0001_initial_schema.sql
@src/tests/data-d1.test.ts
@src/tests/listing-detail-page.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAgentProfileBySlug — profile fields + active visible listings query</name>
  <files>src/lib/data.ts, src/tests/agent-profile-query.test.ts</files>
  <behavior>
    - data.ts exports `getAgentProfileBySlug(slug: string)` returning `null` when no agent matches the slug OR the matched agent has is_suspended=1; otherwise returns an object with public-safe fields (display_name, photo_url, brokerage, license_number, slug) and a `listings: Listing[]` array.
    - The agent lookup selects ONLY public-safe columns — email and phone are NOT selected (defense in depth: the data layer never returns PII on the profile path).
    - The listings query reuses the existing two-query image grouping and applies AGENT_VISIBLE_SQL (from Plan 01) plus `l.status = 'active'`, scoped to that agent (l.agent_id = resolved id), ordered created_at DESC, mapped via rowToListing.
    - Fail-safe: D1 errors are caught and the function returns null (mirrors getListingBySlug).
    - Test: data.ts source exports getAgentProfileBySlug, references AGENT_VISIBLE_SQL, selects brokerage/license_number/photo_url, and does NOT select agents.email or agents.phone in the profile lookup.
  </behavior>
  <action>Per CONTEXT Public Profile Exposure decision (ADMIN-04): add `export async function getAgentProfileBySlug(slug)` to src/lib/data.ts. Step 1: SELECT id, display_name, photo_url, brokerage, license_number, slug, is_suspended FROM agents WHERE slug = ? (parameterized .bind). Return null if no row or is_suspended === 1. Step 2: reuse the getAllListings listings + two-query image grouping pattern but scope WHERE l.agent_id = ? AND l.status = 'active' AND ${AGENT_VISIBLE_SQL}, ordered created_at DESC, mapping rows via the existing rowToListing helper. Return { display_name, photo_url, brokerage, license_number, slug, listings }. NEVER select agents.email or agents.phone. Wrap in try/catch returning null on D1 error. Write src/tests/agent-profile-query.test.ts (source-grep style). Do NOT change the existing public read functions (Plan 01 already updated them).</action>
  <verify>
    <automated>npm run typecheck && node --test src/tests/agent-profile-query.test.ts</automated>
  </verify>
  <done>getAgentProfileBySlug exists, applies the visibility gate, omits email/phone, returns null for missing/suspended agents; agent-profile-query.test.ts passes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: /agents/[slug] public profile page + AgentProfileHeader</name>
  <files>src/app/agents/[slug]/page.tsx, src/components/AgentProfileHeader.tsx, src/tests/agent-profile-page.test.ts</files>
  <behavior>
    - page.tsx exports `dynamic = 'force-dynamic'` and does NOT export `runtime = 'edge'` (per CONTEXT: force-dynamic only on the page). Next.js 15 async params: `const { slug } = await params`.
    - The RSC calls getAgentProfileBySlug(slug); when it returns null it calls notFound() (unknown slug or suspended agent → 404).
    - Renders AgentProfileHeader (display_name, photo_url, brokerage, license_number) and a responsive grid of ListingCard for profile.listings; shows an empty-state message when listings is empty. Public Header/Footer chrome applies as on other public pages.
    - AgentProfileHeader.tsx is a pure presentation component (no 'use client' needed) with an AgentProfileHeaderProps interface exposing ONLY display_name/photo_url/brokerage/license_number — no email/phone props. Photo uses next/image with descriptive alt; reuse existing Tailwind utilities (.card, .container-custom, primary-*).
    - Page metadata title from display_name; robots allow indexing (public marketing page).
    - Test: page.tsx source contains `force-dynamic`, does NOT contain `runtime = 'edge'`, calls getAgentProfileBySlug and notFound, maps ListingCard; AgentProfileHeader interface has no email/phone field.
  </behavior>
  <action>Per CONTEXT Public Profile Exposure + Suspension decisions (ADMIN-04): create src/app/agents/[slug]/page.tsx as a force-dynamic RSC (export const dynamic = 'force-dynamic'; do NOT add runtime='edge' — pages stay force-dynamic only). Await params for the slug, call getAgentProfileBySlug, call notFound() from next/navigation on null. Create src/components/AgentProfileHeader.tsx as a pure presentation component with an AgentProfileHeaderProps interface exposing ONLY display_name, photo_url, brokerage, license_number (no email/phone fields — PII never reaches the client). Render the header + a ListingCard grid (reuse @/components/ListingCard) with an empty-state message; follow existing public-page styling conventions (.container-custom, .card, primary-* palette) and use the listing detail page as a structural reference. Add indexable page metadata derived from display_name. Write src/tests/agent-profile-page.test.ts (source-grep style) asserting force-dynamic present, runtime='edge' absent, notFound + getAgentProfileBySlug + ListingCard usage, and AgentProfileHeaderProps omits email/phone. Do NOT add a login/session gate (public page).</action>
  <verify>
    <automated>npm run typecheck && node --test src/tests/agent-profile-page.test.ts</automated>
  </verify>
  <done>/agents/[slug] renders the profile (no PII) with active visible listings, 404s on unknown/suspended, is force-dynamic without runtime='edge'; agent-profile-page.test.ts passes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green gate</name>
  <files>(no new files — regression gate)</files>
  <action>Run the full suite + typecheck to confirm the new query and page did not regress existing tests. Keep the suite green (fail 0); the new tests increase the total above the post-Plan-01 count.</action>
  <verify>
    <automated>npm run typecheck && npm test 2>&1 | grep -E "(tests|pass|fail) [0-9]+"</automated>
  </verify>
  <done>npm test reports fail 0; typecheck exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The public /agents/[slug] profile page and its data query. Live rendering and visual confirmation are deferred (autonomous-run directive).</what-built>
  <how-to-verify>
    1. In wrangler dev, visit /agents/[slug] for a real agent (after the Plan 01 slug backfill is applied) — confirm name, photo, brokerage, license number, and their active listings render with no email/phone anywhere on the page or in page source.
    2. Visit /agents/unknown-slug — confirm a 404.
    3. Suspend that agent (is_suspended=1) and reload — confirm the profile now 404s; unsuspend and confirm it returns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer/public → /agents/[slug] | Unauthenticated public request reads agent + listings from D1; slug is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Public profile leaks agent email/phone (PII) | mitigate | getAgentProfileBySlug selects only display_name/photo_url/brokerage/license_number; AgentProfileHeaderProps has no email/phone field — PII never reaches the client |
| T-05-07 | Information Disclosure | Suspended agent's profile/listings still reachable | mitigate | Query returns null when is_suspended=1; listings WHERE applies AGENT_VISIBLE_SQL; page calls notFound() |
| T-05-08 | Tampering | Slug as untrusted input enables SQL injection | mitigate | Slug bound via prepare().bind() — no string concatenation |
| T-05-09 | Information Disclosure | XSS via attacker-controlled photo_url | mitigate | photo_url passes isSafeHttpUrl on profile write (Phase 2 / Plan 01); rendered via next/image src |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan; no install tasks |
</threat_model>

<verification>
- `npm run typecheck` clean; `npm test` fail 0.
- getAgentProfileBySlug never selects email/phone and applies the visibility gate.
- page.tsx is force-dynamic with NO runtime='edge'; 404s on unknown/suspended slug.
</verification>

<success_criteria>
- Every non-suspended agent has a public /agents/[slug] page showing public-safe identity + active visible listings (ADMIN-04).
- No PII (email/phone) is exposed on the profile.
- Suite stays green.
</success_criteria>

<output>
Create `.planning/phases/05-admin-panel-agent-profiles/05-02-SUMMARY.md` when done.
</output>
