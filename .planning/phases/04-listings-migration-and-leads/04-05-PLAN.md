---
phase: 04-listings-migration-and-leads
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/(dashboard)/dashboard/listings/page.tsx
  - src/components/dashboard/ListingsManager.tsx
  - src/components/dashboard/ListingForm.tsx
  - src/tests/dashboard-listings.test.ts
autonomous: false
requirements: [LIST-01, LIST-02, LIST-05, LIST-08]
must_haves:
  truths:
    - "An agent sees a table of their own listings with status in the dashboard"
    - "An agent can open a form to create a listing with multiple photo URLs"
    - "An agent can edit and delete their own listings from the dashboard"
    - "An agent can pause/activate a listing from the dashboard"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/listings/page.tsx"
      provides: "force-dynamic RSC shell that lists own listings"
      contains: "force-dynamic"
    - path: "src/components/dashboard/ListingsManager.tsx"
      provides: "client table + create/edit/delete/pause actions"
      contains: "use client"
    - path: "src/components/dashboard/ListingForm.tsx"
      provides: "create/edit form with multiple photo URL inputs"
      contains: "use client"
  key_links:
    - from: "src/components/dashboard/ListingsManager.tsx"
      to: "/api/agent/listings"
      via: "fetch GET/POST/PUT/DELETE/PATCH"
      pattern: "/api/agent/listings"
    - from: "src/app/(dashboard)/dashboard/listings/page.tsx"
      to: "listings (D1)"
      via: "SELECT own listings by agent_id"
      pattern: "agent_id"
---

<objective>
Replace the Phase 2 listings placeholder with a real dashboard listing manager: a force-dynamic RSC
shell that loads the agent's own listings, a client ListingsManager table with create/edit/delete and
pause/activate actions wired to the 04-02 CRUD API, and a ListingForm supporting multiple photo URLs.
Reuses the Phase 2 ProfileForm visual language and dashboard table styling.

This is the agent-facing UI layer over the listing CRUD routes from plan 04-02 (Wave 2). It runs in
Wave 3 because it consumes those endpoints.

Purpose: Give agents a self-serve UI to manage their listings end to end.
Output: Listings dashboard page, ListingsManager + ListingForm client components, UI tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listings-migration-and-leads/04-CONTEXT.md
@.planning/phases/04-listings-migration-and-leads/04-RESEARCH.md
@.planning/phases/02-auth-agent-onboarding/02-UI-SPEC.md
@src/app/(dashboard)/dashboard/listings/page.tsx
@src/app/(dashboard)/layout.tsx
@src/components/InquiryForm.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Listings dashboard RSC shell + ListingsManager table with status actions (LIST-05, LIST-08)</name>
  <files>src/app/(dashboard)/dashboard/listings/page.tsx, src/components/dashboard/ListingsManager.tsx</files>
  <behavior>
    - page.tsx is a force-dynamic, runtime='edge' RSC: getTokens uid → SELECT the agent's own listings (id,title,slug,address,price,status,created_at) WHERE agent_id = uid → pass to <ListingsManager initialListings={...} /> (LIST-08).
    - ListingsManager ('use client') renders a table mirroring the dashboard styling: columns Title/Address, Price, Status badge, Actions (Edit, Delete, Pause/Activate).
    - Pause/Activate calls PATCH /api/agent/listings/[id] { status } and updates the row (LIST-05); Delete calls DELETE with a confirm prompt; both refresh the table on success.
    - Empty state with a prominent "Create listing" CTA when the agent has no listings.
  </behavior>
  <action>Rewrite src/app/(dashboard)/dashboard/listings/page.tsx as a force-dynamic runtime='edge' RSC using the src/app/(dashboard)/layout.tsx D1 pattern: derive uid from getTokens, query the agent's own listings WHERE agent_id = ? (LIST-08), and render <ListingsManager initialListings={listings} />. Create src/components/dashboard/ListingsManager.tsx ('use client') with a table styled per 02-UI-SPEC (card/table conventions, status badge colors for active vs paused) and an Actions column. Wire: Pause/Activate → PATCH /api/agent/listings/${id} with the toggled status (LIST-05); Delete → window.confirm then DELETE /api/agent/listings/${id}; Edit → open ListingForm (Task 2) in edit mode; Create → open ListingForm in create mode. Re-fetch GET /api/agent/listings after any mutation to refresh. Include an empty state with a Create CTA. Create src/tests/dashboard-listings.test.ts asserting the manager fetches /api/agent/listings, has Edit/Delete/Pause actions, and the page scopes its query by agent_id.</action>
  <verify>
    <automated>grep -q '/api/agent/listings' src/components/dashboard/ListingsManager.tsx && grep -q 'agent_id' "src/app/(dashboard)/dashboard/listings/page.tsx" && grep -q "force-dynamic" "src/app/(dashboard)/dashboard/listings/page.tsx" && npm run typecheck</automated>
  </verify>
  <done>The listings dashboard lists the agent's own listings with status badges and Edit/Delete/Pause-Activate actions wired to the CRUD API; empty state present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ListingForm — create/edit with multiple photo URL inputs (LIST-01, LIST-02)</name>
  <files>src/components/dashboard/ListingForm.tsx, src/tests/dashboard-listings.test.ts</files>
  <behavior>
    - 'use client' form reusing the InquiryForm/ProfileForm visual language (labels, inputs, error banners, submit button).
    - Fields: title, address, city, state, zip, price, beds, baths, sqft, description, and a dynamic list of photo URL inputs (add/remove rows) — at least one required (LIST-01 multiple photos).
    - Create mode → POST /api/agent/listings; Edit mode (seeded with an existing listing) → PUT /api/agent/listings/[id] (LIST-02 own-listing edit). On 403 from the API, surface a clear "you can only edit your own listings" error.
    - Client validation mirrors server (required fields, numeric price/beds/baths, http(s) photo URLs); success closes the form and triggers the manager refresh callback.
  </behavior>
  <action>Create src/components/dashboard/ListingForm.tsx ('use client') styled per 02-UI-SPEC ProfileForm patterns. Render the listing fields plus a repeatable photo-URL input group with add/remove buttons (array state), requiring at least one valid http(s) URL (LIST-01). Accept props: mode ('create'|'edit'), an optional existing listing to seed edit mode, and onSuccess callback. Submit: create → fetch POST /api/agent/listings; edit → fetch PUT /api/agent/listings/${id} (LIST-02). Map API error responses (400 validation, 403 forbidden, 409 slug collision) to inline error banners using the InquiryForm status-banner pattern. On success, call onSuccess (manager re-fetches). Add client-side validation matching the server's required fields and URL allowlist so users get instant feedback. Extend src/tests/dashboard-listings.test.ts to assert ListingForm posts to /api/agent/listings on create and PUTs on edit, and supports multiple photo URL inputs.</action>
  <verify>
    <automated>grep -q '/api/agent/listings' src/components/dashboard/ListingForm.tsx && grep -qE "method: ?'(POST|PUT)'" src/components/dashboard/ListingForm.tsx && npm run typecheck</automated>
  </verify>
  <done>ListingForm creates (POST) and edits (PUT) listings with a dynamic multi-photo-URL list, client validation, and mapped API error states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The /dashboard/listings page now lists the agent's own listings and offers create (with multiple photo URLs), edit, delete, and pause/activate — all wired to the 04-02 CRUD API. Live D1 CRUD round-trips are deferred to this human check (autonomous run validated typecheck + source wiring only).</what-built>
  <how-to-verify>
    1. Run wrangler dev with migrations applied; log in as an agent whose subscription is active (or admin).
    2. Open /dashboard/listings → create a new listing with all fields and 3 photo URLs. Confirm it saves and appears in the table, then appears on the public /listings page.
    3. Edit the listing (change price + add a 4th photo) → confirm the change persists and the detail page reflects it.
    4. Pause the listing → confirm the status badge flips and it disappears from public /listings; Activate → it returns.
    5. Delete the listing → confirm it leaves the table and 404s publicly.
    6. (Ownership) As a different agent, attempt to edit/delete the first agent's listing via the API → confirm 403 (cannot be done from the UI, which only lists own listings; verify via direct API call).
    7. As a lapsed agent, attempt to create → confirm the 403 "active subscription required" path.
  </how-to-verify>
  <resume-signal>Type "approved" if create/edit/delete/pause work, public pages reflect changes, and ownership/publishability gates hold — or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent browser → dashboard listings UI | Authenticated; UI only ever lists/edits the signed-in agent's listings |
| UI fetch → /api/agent/listings | All authorization enforced server-side (04-02); UI is not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | ListingsManager actions | mitigate | All mutations hit 04-02 routes which re-check ownership/publishability server-side; UI cannot bypass the gate |
| T-04-17 | Information Disclosure | listings dashboard query | mitigate | RSC SELECT scoped WHERE agent_id = session uid (LIST-08); never lists other agents' listings |
| T-04-18 | Tampering | photo URL inputs | mitigate | Client allowlist for UX; authoritative isSafeHttpUrl check runs server-side in 04-02 |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase — no install task |
</threat_model>

<verification>
- npm run typecheck passes for the page and both client components.
- node --test src/tests/dashboard-listings.test.ts passes (fetch wiring + actions + multi-photo).
- Human checkpoint confirms full create/edit/delete/pause round-trips and gate behavior against live D1.
</verification>

<success_criteria>
- Agents manage their own listings end-to-end from the dashboard (list, create, edit, delete, pause/activate).
- Create form supports multiple photo URLs.
- All authorization remains enforced by the server routes; the UI lists only the agent's own listings.
</success_criteria>

<output>
Create `.planning/phases/04-listings-migration-and-leads/04-05-SUMMARY.md` when done
</output>
