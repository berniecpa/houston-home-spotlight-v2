---
phase: 04-listings-migration-and-leads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/0002_add_featured_column.sql
  - db/migrations/0003_seed_legacy_listings.sql
  - src/lib/data.ts
  - src/tests/listing-detail-page.test.ts
  - src/tests/data-d1.test.ts
  - .env.local.example
autonomous: true
requirements: [LIST-04, LIST-06, LIST-07]
must_haves:
  truths:
    - "getAllListings/getListingBySlug read from D1 and apply the subscription gate"
    - "Hidden/lapsed/paused listings are excluded from public D1 reads"
    - "The featured column exists on listings before any seed runs"
    - "A re-runnable seed migration inserts the 3 legacy listings with exact slugs preserved"
    - "npm test reports 0 failing tests"
  artifacts:
    - path: "db/migrations/0002_add_featured_column.sql"
      provides: "featured INTEGER column on listings"
      contains: "ALTER TABLE listings ADD COLUMN featured"
    - path: "db/migrations/0003_seed_legacy_listings.sql"
      provides: "idempotent seed of 3 legacy listings + images"
      contains: "INSERT OR IGNORE INTO listings"
    - path: "src/lib/data.ts"
      provides: "D1-backed listing read functions with subscription gate"
      contains: "AGENT_PUBLISHABLE_SQL"
      min_lines: 100
    - path: ".env.local.example"
      provides: "RESEND_API_KEY, LEAD_FROM_EMAIL, ADMIN_NOTIFY_EMAIL documented"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "src/lib/data.ts"
      to: "src/lib/subscription.ts"
      via: "AGENT_PUBLISHABLE_SQL import embedded in SELECT WHERE"
      pattern: "AGENT_PUBLISHABLE_SQL"
    - from: "src/lib/data.ts"
      to: "@opennextjs/cloudflare"
      via: "getCloudflareContext({ async: true })"
      pattern: "getCloudflareContext"
---

<objective>
Convert the listing read path from static JSON to live Cloudflare D1, add the `featured` column,
author the idempotent seed migration for the 3 legacy listings, document the new Resend env vars,
and bring the test suite toward 0 failures by updating the stale `listing-detail-page` assertions.

This is the foundation plan for Phase 4 — every other plan (CRUD, public pages, leads) depends on
the D1-backed data layer this plan delivers. It keeps the exact async signatures of `src/lib/data.ts`
so consuming pages/components do not change shape (the D1 swap is internal).

Purpose: Make D1 the single source of truth for listings while preserving the public Listing contract.
Output: 0002/0003 migrations, reworked src/lib/data.ts, updated tests, new env-var docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listings-migration-and-leads/04-CONTEXT.md
@.planning/phases/04-listings-migration-and-leads/04-RESEARCH.md
@src/lib/data.ts
@src/lib/subscription.ts
@src/types/index.ts
@db/migrations/0001_initial_schema.sql
@src/tests/listing-detail-page.test.ts
@src/app/listings/[slug]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add featured column migration + idempotent legacy seed migration (LIST-04, LIST-06, LIST-07)</name>
  <files>db/migrations/0002_add_featured_column.sql, db/migrations/0003_seed_legacy_listings.sql</files>
  <behavior>
    - 0002 ALTERs listings to add `featured INTEGER NOT NULL DEFAULT 0`; numbered so it applies BEFORE 0003 (order matters — 0003 sets featured).
    - 0003 is idempotent: every INSERT uses `INSERT OR IGNORE` keyed on the UNIQUE slug; re-running adds no duplicates.
    - 0003 inserts the 3 legacy listings preserving EXACT slugs: heights-bungalow-historic, riverside-terrace-modern-craftsman, sugarland-estate-pool (LIST-07).
    - agent_id resolves via `(SELECT id FROM agents WHERE is_admin = 1 LIMIT 1)` (no hardcoded uid).
    - title derived deterministically from address; status='active'; only riverside-terrace gets featured=1.
    - images seeded into listing_images with display_order = original JSON array index (LIST-06).
  </behavior>
  <action>Create db/migrations/0002_add_featured_column.sql adding a NOT NULL DEFAULT 0 `featured` INTEGER column to listings — this must be a lower migration number than the seed so the seed's featured values resolve. Create db/migrations/0003_seed_legacy_listings.sql per RESEARCH Domain 4: three `INSERT OR IGNORE INTO listings (...)` statements with agent_id `(SELECT id FROM agents WHERE is_admin = 1 LIMIT 1)`, exact preserved slugs, derived titles ('Modern Craftsman at 2611 Wichita Street', 'Historic Bungalow at 1421 Ashland Street', 'Estate with Pool at 1523 Riverstone Ranch Drive'), status 'active', featured=1 only for riverside-terrace, created_at via `strftime('%s', '<ISO>')` from each JSON createdAt, and `INSERT OR IGNORE INTO listing_images` rows with display_order = array index. Use the exact field values from the three src/data/listings/*.json files (read them for price/beds/baths/sqft/description/zip/images/video_url). Add a header comment documenting the PREREQUISITE: Bernard must log in once and set-admin-claim must have run (is_admin=1) before applying 0003 — the FILE is created now; APPLICATION is a deferred human step. Do NOT apply the migration in this task.</action>
  <verify>
    <automated>grep -v '^--' db/migrations/0002_add_featured_column.sql | grep -c 'ADD COLUMN featured' | grep -qv '^0$' && grep -v '^--' db/migrations/0003_seed_legacy_listings.sql | grep -c 'INSERT OR IGNORE INTO listings' | grep -q '^3$' && grep -c 'heights-bungalow-historic\|riverside-terrace-modern-craftsman\|sugarland-estate-pool' db/migrations/0003_seed_legacy_listings.sql | grep -qv '^0$' && echo MIGRATIONS_OK</automated>
  </verify>
  <done>0002 adds the featured column; 0003 is idempotent (INSERT OR IGNORE), preserves all 3 exact slugs, resolves admin agent_id via subquery, seeds images with display_order, and documents the admin-row prerequisite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework src/lib/data.ts to D1 with subscription gate; keep async signatures (LIST-04, LIST-06)</name>
  <files>src/lib/data.ts, src/tests/data-d1.test.ts</files>
  <behavior>
    - getAllListings/getListingBySlug/getFeaturedListings/filterListings keep their exact async signatures and return the Listing shape from src/types/index.ts.
    - Public SELECTs JOIN agents a and embed AGENT_PUBLISHABLE_SQL plus `l.status = 'active'` — paused/lapsed/hidden listings are excluded (LIST-04 gate).
    - Images use TWO queries + in-memory Map grouping by listing_id ordered by display_order (no N-row JOIN fanout) (LIST-06).
    - D1 row → Listing mapping: zip ?? '', sqft ?? 0, description ?? '', videoUrl from video_url, featured === 1, createdAt from epoch seconds → ISO string.
    - clearListingsCache remains exported as a no-op for test compatibility.
    - On D1 error, functions catch + log + return [] / null (fail safe, never throw to the page).
  </behavior>
  <action>Rewrite src/lib/data.ts following RESEARCH Domain 2 exactly: import getCloudflareContext from '@opennextjs/cloudflare' and AGENT_PUBLISHABLE_SQL from '@/lib/subscription'. Implement getAllListings (active+publishable, two-query image grouping, ORDER BY created_at DESC), getListingBySlug (same gate, slug-bound, returns null when hidden/missing so the detail page 404s — LIST-04), getFeaturedListings (filter all by featured), filterListings (in-memory price/beds filter over getAllListings), and a no-op clearListingsCache. Map epoch `created_at` to ISO via `new Date(row.created_at * 1000).toISOString()`. Remove all JSON imports and the module-level listingsCache. Write src/tests/data-d1.test.ts as source-grep assertions (consistent with the existing test style — no live D1 in node --test): assert data.ts imports getCloudflareContext and AGENT_PUBLISHABLE_SQL, that getListingBySlug's query contains AGENT_PUBLISHABLE_SQL and l.status = 'active', that no `@/data/listings` import remains, and that all four async function signatures are still exported.</action>
  <verify>
    <automated>npm run typecheck && node --test src/tests/data-d1.test.ts</automated>
  </verify>
  <done>data.ts reads D1 with the subscription gate applied, uses two-query image grouping, keeps identical async signatures, drops JSON imports, and data-d1.test.ts passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update the 4 stale listing-detail-page tests + document Resend env vars (LIST-04)</name>
  <files>src/tests/listing-detail-page.test.ts, .env.local.example</files>
  <action>The detail page at src/app/listings/[slug]/page.tsx already uses the correct Next.js 15 async params shape; the failing assertions are stale (they expect the old sync `params.slug` pattern and a present generateStaticParams). Per RESEARCH Domain 6, update src/tests/listing-detail-page.test.ts assertions to the async shape WITHOUT touching the page: (a) change the generateStaticParams suite to assert it is ABSENT (force-dynamic removes static generation) and drop the getAllListings-import expectation; (b) change generateMetadata + Page Component params assertions from `params: { slug: string }` to `params: Promise<{ slug: string }>`; (c) change `getListingBySlug(params.slug)` assertions to expect `const { slug } = await params` plus `getListingBySlug(slug)`; (d) update the imports test to expect only `getListingBySlug` from '@/lib/data' (getAllListings no longer imported by the detail page). Note: the page's force-dynamic conversion and generateStaticParams removal happen in plan 04-03; these test edits anticipate that final source shape, so the full suite reaches green only after 04-03 — this task's bar is that the assertion STRINGS match the documented target source. Separately, append RESEND_API_KEY, LEAD_FROM_EMAIL, and ADMIN_NOTIFY_EMAIL to .env.local.example with brief comments (verified domain for LEAD_FROM_EMAIL; ADMIN_NOTIFY_EMAIL is Bernard's CC) per RESEARCH "New Env Vars Required".</action>
  <verify>
    <automated>grep -c 'params: Promise<{ slug: string }>' src/tests/listing-detail-page.test.ts | grep -qv '^0$' && ! grep -q 'getListingBySlug(params.slug)' src/tests/listing-detail-page.test.ts && grep -q 'RESEND_API_KEY' .env.local.example && grep -q 'ADMIN_NOTIFY_EMAIL' .env.local.example && echo TESTS_AND_ENV_OK</automated>
  </verify>
  <done>The 4 stale assertions now target the async-params / no-generateStaticParams source; .env.local.example documents the three new Resend vars. (Full-suite green is sealed by 04-03's page conversion.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public buyer → RSC data layer | Unauthenticated reads must only surface publishable+active listings |
| migration author → D1 | Seed SQL must not allow duplicate/invalid rows on re-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | getAllListings/getListingBySlug | mitigate | Embed AGENT_PUBLISHABLE_SQL + l.status='active' in every public SELECT; lapsed/paused listings never returned |
| T-04-02 | Tampering | 0003 seed migration | mitigate | INSERT OR IGNORE keyed on UNIQUE slug — re-run is a no-op, no duplicate rows |
| T-04-03 | Tampering | data.ts D1 queries | mitigate | Parameterized .bind() for slug; AGENT_PUBLISHABLE_SQL is a constant fragment, never interpolated user input |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (RESEARCH Package Legitimacy Audit) — no install task |
</threat_model>

<verification>
- npm run typecheck passes (D1 row types map cleanly to Listing).
- node --test src/tests/data-d1.test.ts passes.
- Migrations 0002/0003 exist with the asserted content; 0003 is idempotent and preserves slugs.
- .env.local.example documents the three Resend vars.
</verification>

<success_criteria>
- src/lib/data.ts reads D1 with the Phase 3 subscription gate, identical async signatures, two-query image grouping.
- featured column migration precedes the idempotent seed; 3 legacy slugs preserved exactly.
- Stale detail-page test assertions updated to the async-params target shape.
</success_criteria>

<output>
Create `.planning/phases/04-listings-migration-and-leads/04-01-SUMMARY.md` when done
</output>
