---
phase: 06-ai-video-generation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/api/agent/listings/[id]/video/route.ts
  - src/app/api/agent/listings/[id]/video-status/route.ts
  - src/app/api/video/callback/route.ts
  - src/tests/video-trigger-api.test.ts
  - src/tests/video-callback-api.test.ts
autonomous: true
requirements: [VIDEO-01, VIDEO-02, VIDEO-03, VIDEO-04]

must_haves:
  truths:
    - "An agent can POST to trigger video generation on their OWN listing and get a <2s 202 response with a jobId (VIDEO-01)"
    - "Cross-agent triggers return 403; unpublishable/suspended agents are blocked; listings with no photo URL return 400 (VIDEO-01)"
    - "A duplicate trigger while a job is active returns the in-flight job (409) — never a second concurrent job (VIDEO-04)"
    - "The Kie.ai callback verifies HMAC, parses the video URL, and idempotently writes listings.video_url + video_status (VIDEO-02)"
    - "The status endpoint returns the current job status + videoUrl for client polling (VIDEO-02/03)"
  artifacts:
    - path: "src/app/api/agent/listings/[id]/video/route.ts"
      provides: "POST trigger: authz gate + dedup + async submit + 202"
      exports: ["POST"]
    - path: "src/app/api/agent/listings/[id]/video-status/route.ts"
      provides: "GET status poll endpoint"
      exports: ["GET"]
    - path: "src/app/api/video/callback/route.ts"
      provides: "POST Kie.ai callback: HMAC verify + idempotent result write (middleware-exempt)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/agent/listings/[id]/video/route.ts"
      to: "src/lib/video/provider.ts + jobs.ts"
      via: "submitWithFallback + insertJob/findActiveJob"
      pattern: "submitWithFallback|findActiveJob"
    - from: "src/app/api/video/callback/route.ts"
      to: "src/lib/video/kie-adapter.ts + jobs.ts"
      via: "verifyKieSignature + applyTerminalResult"
      pattern: "verifyKieSignature|applyTerminalResult"
---

<objective>
Build the three HTTP routes that drive the happy path: the agent trigger (authz +
dedup + async submit), the server-to-server Kie.ai callback (HMAC verify + idempotent
result write, middleware-exempt), and the lightweight status endpoint the dashboard
polls. All gates and provider/job logic are reused from 06-01.

Purpose: Turn the 06-01 libraries into a working async pipeline — submit returns in
<2s, completion lands via signed callback, and the client can observe status.

Output: video/route.ts (POST), video-status/route.ts (GET), video/callback/route.ts (POST),
and source-grep tests for authz, dedup, async-return, HMAC, and idempotency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-video-generation/06-RESEARCH.md

# Patterns to reuse (ownership preamble, suspension gate, webhook crypto, middleware matcher)
@src/app/api/agent/listings/[id]/route.ts
@src/app/api/stripe/webhook/route.ts
@src/lib/subscription.ts
@middleware.ts
@src/lib/listings-db.ts

# Contracts produced by 06-01 (consume, do not redefine)
@.planning/phases/06-ai-video-generation/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate-Video trigger route + status endpoint</name>
  <files>src/app/api/agent/listings/[id]/video/route.ts, src/app/api/agent/listings/[id]/video-status/route.ts, src/tests/video-trigger-api.test.ts</files>
  <action>
Create src/app/api/agent/listings/[id]/video/route.ts exporting `POST` and
`export const runtime = 'edge'` (API routes may use edge; pages must NOT — see the existing
[id]/route.ts). Reuse the ownership + suspension pattern from
src/app/api/agent/listings/[id]/route.ts:
1. Authz: derive uid from getTokens(cookieStore, authEdgeConfig); 401 if no tokens; 403 if
   !email_verified. Ownership: SELECT agent_id FROM listings WHERE id=? — 404 if absent, 403 if
   agent_id !== uid (cross-agent — VIDEO-01 / T-06-05). Suspension gate via the same checkSuspended
   pattern (403). Publishability: getAgentSubscriptionState + isAgentPublishable from
   @/lib/subscription — 403 if not publishable (D-Trigger Authz).
2. Photo gate: SELECT the first image URL `SELECT url FROM listing_images WHERE listing_id=?
   ORDER BY display_order ASC LIMIT 1`; 400 "Listing has no photo to animate." if none. Validate
   with isSafeHttpUrl (T-06-06 SSRF — only http(s) URLs reach the provider).
3. DEDUP (VIDEO-04): findActiveJob(db, listingId); if a row exists return the in-flight job with
   409 { jobId: existing.id, status: existing.status } — never spawn a second.
4. Async submit (VIDEO-01, must return <2s): insertJob(db,{id:crypto.randomUUID(), listingId,
   agentId:uid, provider:'kie'}) with status='processing'; build callbackUrl =
   `${siteConfig.url}/api/video/callback` (import siteConfig from @/app/layout); call
   submitWithFallback(env, imageUrl, callbackUrl) wrapped in try/catch with a 5s AbortController
   timeout — on success setTaskId(db, jobId, taskId) and recordAttempt with the winning provider;
   on throw, LEAVE the job 'processing' so the poller (06-03) retries via HiggsField (do NOT 500
   the agent). Return 202 { jobId, status:'processing' }. Also UPDATE listings SET
   video_status='processing' so the dashboard reflects in-flight state.
Wrap the handler in try/catch returning typed NextResponse 500 on unexpected error (match the
existing { success:false, message } convention).

Create src/app/api/agent/listings/[id]/video-status/route.ts exporting `GET` +
`export const runtime = 'edge'`. Reuse the ownership preamble (401/403/404) so only the owner can
read their job status. SELECT the latest video_jobs row for the listing (`WHERE listing_id=? ORDER
BY updated_at DESC LIMIT 1`) and listings.video_url/video_status; return { status, videoUrl }. If
no job, return { status: video_status ?? 'none', videoUrl }. This is the endpoint the dashboard
polls every 3-5s (06-04).

Create src/tests/video-trigger-api.test.ts (source-grep): assert video/route.ts exports POST and
runtime='edge'; reuses getTokens + ownership SELECT agent_id + 403 cross-agent; calls
isAgentPublishable and checkSuspended; has the no-photo 400 + isSafeHttpUrl; calls findActiveJob
and returns 409 on an active job; calls insertJob + submitWithFallback + setTaskId; returns 202;
does NOT return 500 when submit throws (the catch leaves the job processing). Assert
video-status/route.ts exports GET, reuses the ownership preamble, and returns status + videoUrl.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --test src/tests/video-trigger-api.test.ts</automated>
  </verify>
  <done>POST trigger enforces ownership+publishable+not-suspended+has-photo, dedups via findActiveJob (409), submits async and returns 202 in <2s without blocking on provider failure; GET status endpoint returns job status+videoUrl scoped to owner; tests pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Kie.ai callback route (HMAC verify + idempotent result write)</name>
  <files>src/app/api/video/callback/route.ts, src/tests/video-callback-api.test.ts</files>
  <action>
Create src/app/api/video/callback/route.ts exporting `POST`. Mirror the Stripe webhook route
structure (src/app/api/stripe/webhook/route.ts) for a server-to-server handler:
1. Read the RAW body FIRST: `const raw = await req.text()` (must be the first body access — same
   rule as the Stripe handler; consuming req.json() first would corrupt HMAC bytes).
2. First-receipt resilience (Open Question 1 / Pitfall 8): `console.log` the raw body once so the
   actual Kling 2.6 callback shape can be confirmed during deferred live testing.
3. HMAC verify (T-06-04): read X-Webhook-Timestamp and X-Webhook-Signature headers; if either is
   absent → 400. Parse taskId from the body (top-level taskId per market-endpoint envelope). Call
   verifyKieSignature(taskId, timestamp, signature, env.KIE_WEBHOOK_SECRET) from
   @/lib/video/kie-adapter; reject 400 on failure. If KIE_WEBHOOK_SECRET is unset → 400 (refuse
   unauthenticated callbacks — Pitfall 1; never trust an unsigned completion).
4. Determine outcome from the body: code===200 → ready, extract videoUrl via
   extractKieCallbackVideoUrl(body) (dual-parse video_url / resultJson); code!==200 → failed
   (capture msg/failMsg into error).
5. Idempotent write (VIDEO-02/04): call applyTerminalResult(db, taskId, outcome). If it returns
   {applied:false} (already terminal — duplicate Kie.ai retry, Pitfall 2) return 200 no-op. On
   success applyTerminalResult writes listings.video_url + video_status (already URL-validated by
   isSafeHttpUrl inside jobs.ts — T-06-02). Return 200 { received: true } so Kie.ai stops retrying.
Do NOT add `export const runtime = 'edge'` (the @opennextjs/cloudflare adapter does not support it
on this route style — match the Stripe webhook which omits it). The route MUST be reachable
server-to-server (no session): it is OUTSIDE the middleware matcher
['/dashboard/:path*','/admin/:path*'] already, so no middleware change is needed — add a code
comment documenting that exemption (like the Stripe webhook comment). Do NOT modify middleware.ts.

Create src/tests/video-callback-api.test.ts (source-grep): assert callback/route.ts reads
`await req.text()` as the FIRST body access (its index precedes any req.json); logs the raw body
once; requires X-Webhook-Timestamp + X-Webhook-Signature and 400s when missing; calls
verifyKieSignature; 400s when KIE_WEBHOOK_SECRET is unset; calls extractKieCallbackVideoUrl; calls
applyTerminalResult and returns 200 no-op on {applied:false}; returns 200 { received: true } on
success; contains the middleware-exemption comment; does NOT contain `runtime = 'edge'`. Also
assert middleware.ts matcher still equals ['/dashboard/:path*','/admin/:path*'] (callback exempt).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --test src/tests/video-callback-api.test.ts</automated>
  </verify>
  <done>Callback reads raw body first, rejects missing/invalid HMAC and missing secret (400), dual-parses the video URL, writes results idempotently via applyTerminalResult (200 no-op on duplicates), returns 200 on success, stays middleware-exempt; tests pass; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent browser → trigger route | Authenticated session; uid must come from cookie, never body |
| Kie.ai servers → callback route | Unauthenticated server-to-server POST; HMAC is the only authenticity proof |
| Listing photo URL → provider | Agent-supplied URL leaves our network to the provider (SSRF surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Spoofing | forged callback POST | mitigate | verifyKieSignature HMAC-SHA256 over `taskId.timestamp`; 400 if header absent or KIE_WEBHOOK_SECRET unset (ASVS V6) |
| T-06-03 | Tampering | replayed/duplicate callback | mitigate | applyTerminalResult guarded `WHERE task_id=? AND status='processing'`; {applied:false} → 200 no-op (VIDEO-04) |
| T-06-05 | Elevation of Privilege | cross-agent video trigger | mitigate | resolveOwnership preamble: SELECT agent_id, 403 when agent_id !== session uid (ASVS V4) |
| T-06-07 | Elevation of Privilege | suspended/unpublishable agent triggers | mitigate | checkSuspended + isAgentPublishable gates reused from Phase 3/5 (ASVS V4) |
| T-06-06 | Tampering | SSRF via pasted photo URL | mitigate | isSafeHttpUrl rejects non-http(s) schemes before the URL is sent to the provider (ASVS V5) |
| T-06-08 | Information Disclosure | status endpoint leaking other agents' jobs | mitigate | status GET reuses the ownership preamble (401/403/404) before any read (ASVS V4) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH "No new npm installs") |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `node --test src/tests/video-trigger-api.test.ts src/tests/video-callback-api.test.ts` pass.
- Full suite stays green: `npm test` ends 0 fail.
- DEFERRED human-verify: live Kie.ai submit + signed callback round-trip; <2s submit timing; cross-agent 403 against live D1.
</verification>

<success_criteria>
- Trigger returns 202 <2s, enforces ownership/publishable/suspension/has-photo, dedups (409) (VIDEO-01, VIDEO-04).
- Callback verifies HMAC, dual-parses URL, writes idempotently, stays middleware-exempt (VIDEO-02).
- Status endpoint returns owner-scoped job status + videoUrl (VIDEO-02/03).
- Suite green; no new npm packages.
</success_criteria>

<output>
Create `.planning/phases/06-ai-video-generation/06-02-SUMMARY.md` when done
</output>
