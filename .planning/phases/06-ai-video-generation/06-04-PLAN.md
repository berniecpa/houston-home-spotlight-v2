---
phase: 06-ai-video-generation
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/dashboard/ListingsManager.tsx
  - src/app/listings/[slug]/page.tsx
  - src/tests/video-ui.test.ts
autonomous: true
requirements: [VIDEO-01, VIDEO-02, VIDEO-03]

must_haves:
  truths:
    - "Each listing row has a Generate Video button that POSTs the trigger and shows submitted confirmation without a >2s spinner (VIDEO-01)"
    - "The dashboard polls the status endpoint every 3-5s and stops on a terminal state, swapping in the video / status (VIDEO-02)"
    - "A failed/retrying job shows 'generation failed — retrying' rather than a silent error (VIDEO-03)"
    - "The public listing detail page renders a real <video> when video_url is present (VIDEO-02)"
  artifacts:
    - path: "src/components/dashboard/ListingsManager.tsx"
      provides: "Generate Video button + status badge + polling loop"
      contains: "video-status"
    - path: "src/app/listings/[slug]/page.tsx"
      provides: "native video render when listing.videoUrl present"
      contains: "<video"
  key_links:
    - from: "src/components/dashboard/ListingsManager.tsx"
      to: "/api/agent/listings/[id]/video + /video-status"
      via: "fetch POST trigger + interval poll"
      pattern: "video-status|/video"
    - from: "src/app/listings/[slug]/page.tsx"
      to: "listing.videoUrl"
      via: "conditional native video element"
      pattern: "listing.videoUrl"
---

<objective>
Wire the agent-facing and buyer-facing UI: a Generate Video button + live status
polling in the dashboard ListingsManager, and a real native video render on the
public listing detail page (replacing the current "Video tour coming soon"
placeholder). Touches only UI files — disjoint from the route files in 06-02, so it
runs in the same wave.

Purpose: Close the user-visible loop for VIDEO-01/02/03 — the agent triggers, sees
live progress, and the finished video appears for buyers.

Output: updated ListingsManager.tsx (button + poll + status), updated detail page.tsx
(native video), and source-grep tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-video-generation/06-CONTEXT.md

# Files modified (read before editing) + Tailwind/a11y conventions to follow
@src/components/dashboard/ListingsManager.tsx
@src/app/listings/[slug]/page.tsx
@src/app/(dashboard)/dashboard/listings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Video button + status polling in ListingsManager</name>
  <files>src/components/dashboard/ListingsManager.tsx, src/tests/video-ui.test.ts</files>
  <action>
Edit src/components/dashboard/ListingsManager.tsx (already a 'use client' component). Add a
per-listing video workflow without breaking existing CRUD:
1. Extend OwnListing with optional `video_status?: 'none'|'processing'|'ready'|'failed'|null` and
   `video_url?: string|null` (add as OPTIONAL so the component compiles whether or not the
   dashboard page SELECT in src/app/(dashboard)/dashboard/listings/page.tsx supplies them — do NOT
   edit that page here, to keep file ownership clean for the wave).
2. Add a "Generate Video" action button in the Actions cell (mirror existing button styling:
   text-xs, touch-target, disabled:opacity-50, aria-label `Generate video for ${listing.title}`).
   Disabled while that listing's video_status is 'processing'.
3. handleGenerateVideo(listing): POST `/api/agent/listings/${listing.id}/video`; on 202 set that
   row's video_status='processing' optimistically and start polling; on 409 adopt the returned
   in-flight job and start polling (do not error); on other non-ok show the existing actionError
   banner with the server message. Keep it fast — no blocking spinner beyond the immediate fetch
   (VIDEO-01).
4. Polling (VIDEO-02): an interval per processing listing that GETs
   `/api/agent/listings/${id}/video-status` every 4000ms (3-5s range, Claude's discretion). On
   { status:'ready', videoUrl } set the row video_status='ready' + video_url and STOP the interval.
   On { status:'failed' } set video_status='failed' and STOP. Cap total polling (e.g. clear after
   ~5 minutes) to avoid an unbounded loop. Always clearInterval on unmount.
5. Status display (VIDEO-03): render a status badge/text per row — 'processing' → "Generating…",
   'failed' → "Generation failed — retrying" (exact phrasing per success criterion 3, NOT a silent
   error), 'ready' → a "View video" affordance (link to the public listing or inline note). Use
   role="status" / aria-live="polite" for the live region.
Preserve all existing create/edit/delete/pause behavior and the existing actionError handling.

Create src/tests/video-ui.test.ts (source-grep, node:test): assert ListingsManager.tsx contains a
Generate Video button (aria-label includes "Generate video"), POSTs to `/video`, handles 409 by
adopting the in-flight job, polls `/video-status` on an interval, clears the interval (clearInterval
present), stops on ready/failed, renders the exact "Generation failed — retrying" text, and uses
aria-live for the status region.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --test src/tests/video-ui.test.ts</automated>
  </verify>
  <done>ListingsManager shows a Generate Video button, fires the async trigger (handles 202 + 409), polls video-status every ~4s and stops on terminal state, shows "Generation failed — retrying" on failure, never blocks >2s; existing CRUD untouched; tests pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Render native video on the public listing detail page</name>
  <files>src/app/listings/[slug]/page.tsx, src/tests/video-ui.test.ts</files>
  <action>
Edit src/app/listings/[slug]/page.tsx. The page already has a `{listing.videoUrl && (...)}` Video
Tour section that currently renders a "Video tour coming soon" placeholder. Replace the placeholder
div with a real responsive native <video> element:
- `<video controls preload="metadata" className="w-full rounded-lg" src={listing.videoUrl}>` inside
  the existing aspect-video container; include a fallback text child for browsers without video
  support and an aria-label referencing the listing address.
- Keep the existing section heading "Video Tour" and card styling (bg-white rounded-xl …).
- The page is already force-dynamic and reads listing.videoUrl from D1 via getListingBySlug — no
  client polling here (per D-Client Live Update: the public page just renders when present). The
  JSON-LD already includes `video: listing.videoUrl` — leave it.
Do NOT change getListingBySlug or the data layer.

Append a describe block to src/tests/video-ui.test.ts (same file as Task 1): assert the detail page
renders a `<video` element with `controls` and `src={listing.videoUrl}` and no longer contains the
"Video tour coming soon" placeholder string.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --test src/tests/video-ui.test.ts</automated>
  </verify>
  <done>Detail page renders a real native <video controls> bound to listing.videoUrl, placeholder removed, styling/heading preserved; tests pass; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → trigger/status routes | Client calls authenticated routes; all authz is server-side (UI cannot bypass) |
| Provider video URL → buyer browser | video_url rendered in a public <video src>; already https-validated server-side before storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Elevation of Privilege | client-side video trigger | mitigate | Button only calls the server route; ownership/publishable/suspension enforced server-side in 06-02 (UI cannot bypass) (ASVS V4) |
| T-06-02 | Tampering | malicious video_url rendered to buyers | mitigate | video_url is isSafeHttpUrl-validated before write (06-01 jobs.ts); only https provider URLs reach the <video src> (ASVS V5) |
| T-06-10 | Denial of Service | unbounded client polling | accept | Poll interval ~4s with a hard cap (~5 min) and clearInterval on terminal/unmount; low-volume agent dashboards (Pitfall 6 cadence rationale) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH "No new npm installs") |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `node --test src/tests/video-ui.test.ts` pass.
- Full suite stays green: `npm test` ends 0 fail.
- DEFERRED human-verify: visual check of the button, live polling swap-in, and buyer-facing video playback in wrangler dev / preview.
</verification>

<success_criteria>
- Generate Video button triggers async job, no >2s spinner (VIDEO-01).
- Dashboard polls every ~4s, stops on terminal state, swaps in status/video (VIDEO-02).
- "Generation failed — retrying" shown on failure, not a silent error (VIDEO-03).
- Public detail page plays the real video when video_url present (VIDEO-02).
- Existing CRUD untouched; suite green; no new npm packages.
</success_criteria>

<output>
Create `.planning/phases/06-ai-video-generation/06-04-SUMMARY.md` when done
</output>
