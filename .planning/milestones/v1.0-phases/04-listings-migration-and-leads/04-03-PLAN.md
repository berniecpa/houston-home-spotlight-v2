---
phase: 04-listings-migration-and-leads
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/listings/page.tsx
  - src/app/listings/ListingsClient.tsx
  - src/app/listings/[slug]/page.tsx
  - src/app/page.tsx
  - src/tests/listings-page.test.ts
autonomous: false
requirements: [LIST-04, LIST-07]
must_haves:
  truths:
    - "The /listings browse page renders D1 listings server-side with filters intact"
    - "The /listings/[slug] detail page is force-dynamic and 404s on hidden/missing slugs"
    - "The home page featured grid reads D1 via getFeaturedListings"
    - "The 3 legacy slugs resolve at their original URLs with no visual regression"
    - "npm test reports 0 failing tests after this plan"
  artifacts:
    - path: "src/app/listings/page.tsx"
      provides: "force-dynamic RSC that fetches D1 listings"
      contains: "force-dynamic"
    - path: "src/app/listings/ListingsClient.tsx"
      provides: "client filter shell receiving initialListings"
      contains: "use client"
    - path: "src/app/listings/[slug]/page.tsx"
      provides: "force-dynamic detail page, no generateStaticParams"
      contains: "force-dynamic"
  key_links:
    - from: "src/app/listings/page.tsx"
      to: "src/lib/data.ts"
      via: "await getAllListings() then pass to ListingsClient"
      pattern: "getAllListings"
    - from: "src/app/listings/[slug]/page.tsx"
      to: "src/lib/data.ts"
      via: "getListingBySlug(slug) → notFound() when null"
      pattern: "getListingBySlug"
---

<objective>
Convert the public listing read path from static JSON / static export to force-dynamic D1 reads with
no visual regression: promote /listings to an RSC that fetches from D1 and delegates filter UI to a
new ListingsClient, make /listings/[slug] force-dynamic (removing generateStaticParams), and keep the
home featured grid reading D1 via the reworked data layer. This plan seals the test suite at 0 failures
by producing the exact detail-page source shape that 04-01's updated assertions target.

Purpose: Public buyers see D1-backed listings with the subscription gate applied; legacy URLs preserved.
Output: RSC browse page + ListingsClient, force-dynamic detail page, verified home grid, updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listings-migration-and-leads/04-CONTEXT.md
@.planning/phases/04-listings-migration-and-leads/04-RESEARCH.md
@src/app/listings/page.tsx
@src/app/listings/[slug]/page.tsx
@src/app/page.tsx
@src/lib/data.ts
@src/components/ListingCard.tsx
@src/components/FilterBar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detail page → force-dynamic, remove generateStaticParams; home grid verified (LIST-04, LIST-07)</name>
  <files>src/app/listings/[slug]/page.tsx, src/app/page.tsx, src/tests/listings-page.test.ts</files>
  <behavior>
    - [slug]/page.tsx exports `export const dynamic = 'force-dynamic'` (NO runtime='edge' on pages — @opennextjs/cloudflare ignores it).
    - generateStaticParams is REMOVED entirely; the getAllListings import is removed (detail page only needs getListingBySlug).
    - The existing async params + `const { slug } = await params` + getListingBySlug(slug) + `if (!listing) notFound()` logic is KEPT as-is (already correct).
    - getListingBySlug returns null for hidden/lapsed/paused slugs (gate in data.ts), so those 404 (LIST-04).
    - page.tsx (home) keeps `await getFeaturedListings()`; verify it renders the D1-backed featured grid with no regression.
  </behavior>
  <action>Edit src/app/listings/[slug]/page.tsx: add `export const dynamic = 'force-dynamic';` near the top, DELETE the entire generateStaticParams function, and change the data import to `import { getListingBySlug } from '@/lib/data';` (drop getAllListings). Leave generateMetadata, the component, async params handling, formatPrice/formatNumber, JSON-LD, and all JSX untouched — no visual change (LIST-07 no-regression). Confirm src/app/page.tsx still calls getFeaturedListings() (now D1-backed via 04-01) and renders the featured grid; add `export const dynamic = 'force-dynamic';` to the home page so the featured grid reads D1 per request. Create src/tests/listings-page.test.ts asserting the browse page (Task 2 output) is force-dynamic and the detail page is force-dynamic with generateStaticParams absent — keeping it in the same source-grep style as the existing suite.</action>
  <verify>
    <automated>grep -q "dynamic = 'force-dynamic'" src/app/listings/[slug]/page.tsx && ! grep -q 'generateStaticParams' src/app/listings/[slug]/page.tsx && ! grep -q 'runtime' src/app/listings/[slug]/page.tsx && npm run typecheck</automated>
  </verify>
  <done>Detail page is force-dynamic with no generateStaticParams and no runtime directive; home page reads featured from D1; new listings-page test created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Browse page → RSC + ListingsClient extraction; filters preserved (LIST-04, LIST-07)</name>
  <files>src/app/listings/page.tsx, src/app/listings/ListingsClient.tsx</files>
  <behavior>
    - page.tsx becomes an RSC: `export const dynamic = 'force-dynamic'`, `const listings = await getAllListings()`, renders <ListingsClient initialListings={listings} />.
    - ListingsClient.tsx is 'use client' and contains the EXACT current filter UX: useState filters, filterListingsClientSide, FilterBar, the results-count copy, empty/no-match states, and the responsive ListingCard grid (LIST-07 no visual regression).
    - Removing the JSON dynamic-import useEffect entirely; data now arrives as a prop. Loading spinner state is dropped (server-rendered).
  </behavior>
  <action>Per RESEARCH Domain 3 / Pitfall P9: rewrite src/app/listings/page.tsx as a server component — remove 'use client', useState/useEffect/useCallback/useMemo and the JSON import block; add `export const dynamic = 'force-dynamic';`, `import { getAllListings } from '@/lib/data';`, `import ListingsClient from './ListingsClient';`, then `const listings = await getAllListings(); return <ListingsClient initialListings={listings} />;`. Create src/app/listings/ListingsClient.tsx as a 'use client' component that receives `initialListings: Listing[]` and reproduces the current page's filter state machine and JSX verbatim (hero, breadcrumb, FilterBar, results count, both empty states, the ListingCard grid) so the buyer-facing UX is byte-for-byte equivalent minus the now-unneeded loading spinner. Keep filterListingsClientSide logic identical.</action>
  <verify>
    <automated>grep -q "dynamic = 'force-dynamic'" src/app/listings/page.tsx && ! grep -q "'use client'" src/app/listings/page.tsx && grep -q "'use client'" src/app/listings/ListingsClient.tsx && grep -q 'initialListings' src/app/listings/ListingsClient.tsx && npm run typecheck</automated>
  </verify>
  <done>/listings is a force-dynamic RSC fetching D1 listings and delegating filters to ListingsClient; the filter UX is preserved with no visual regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The public read path now serves from D1: browse (/listings) is an RSC + ListingsClient, detail (/listings/[slug]) is force-dynamic with the subscription gate, and the home featured grid reads D1. The full test suite (including 04-01's updated detail-page assertions) is green.</what-built>
  <how-to-verify>
    1. Ensure migrations 0002/0003 are applied locally (`npx wrangler d1 migrations apply DB --local`) AND a local admin agents row exists (is_admin=1) so the seed resolves.
    2. Run `wrangler dev` (or `npm run dev` with the OpenNext dev overlay).
    3. Visit /listings — confirm the 3 legacy listings render with the same hero, filters, results count, and card grid as before (no visual regression). Apply a price/beds filter and confirm instant client-side filtering still works.
    4. Visit /listings/heights-bungalow-historic, /listings/riverside-terrace-modern-craftsman, /listings/sugarland-estate-pool — confirm each loads at its original URL with gallery, stats, description, and inquiry form intact.
    5. Visit the home page — confirm the featured grid shows the riverside-terrace listing.
    6. (Gate behavior) Pause a listing in D1 (status='paused') and confirm it disappears from /listings and its slug URL 404s.
    7. Run `npm test` and confirm 0 failing tests.
  </how-to-verify>
  <resume-signal>Type "approved" if the legacy URLs render with no regression and tests pass, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public buyer → RSC pages | Unauthenticated; only publishable+active listings may render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | /listings + /listings/[slug] | mitigate | Read path delegates to data.ts which embeds AGENT_PUBLISHABLE_SQL + status='active'; hidden listings 404/omit |
| T-04-10 | Tampering | JSON-LD on detail page | accept | JSON-LD is built server-side from trusted D1 values; React auto-escapes text; no untrusted script injection path |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase — no install task |
</threat_model>

<verification>
- npm run typecheck passes for all four page/client files.
- npm test reports 0 failing tests (04-01's detail-page assertions now match this final source).
- Detail page is force-dynamic, no generateStaticParams, no runtime directive.
- Browse page is RSC + ListingsClient; filters preserved.
- Human checkpoint confirms legacy URLs render with no visual regression and the gate hides paused listings.
</verification>

<success_criteria>
- Public browse + detail + home featured all read D1 via the gated data layer.
- The 3 legacy slugs resolve at original URLs with no visual regression.
- Test suite green at 0 failures.
</success_criteria>

<output>
Create `.planning/phases/04-listings-migration-and-leads/04-03-SUMMARY.md` when done
</output>
