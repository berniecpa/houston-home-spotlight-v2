---
phase: 03-subscription-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/stripe.ts
  - src/lib/subscription.ts
  - cloudflare-env.d.ts
  - .env.local.example
  - src/tests/subscription.test.ts
  - src/tests/stripe-lib.test.ts
autonomous: true
requirements: [BILL-04, BILL-05]
user_setup:
  - service: stripe
    why: "Subscription billing — $79/mo recurring plan, webhooks, Customer Portal"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys (Secret key, test mode)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard → Developers → Webhooks → signing secret (or `stripe listen` output)"
      - name: STRIPE_PRICE_ID
        source: "Stripe Dashboard → Product catalog → create $79/mo recurring Price → copy price_… id"
    dashboard_config:
      - task: "Create the $79/month recurring Price (single tier, no setup fee, no trial) and copy its price_… id"
        location: "Stripe Dashboard → Product catalog → Add product"

must_haves:
  truths:
    - "isAgentPublishable returns true for an admin (is_admin=1) regardless of subscription_status"
    - "isAgentPublishable returns true for subscription_status='active'"
    - "isAgentPublishable returns true for 'grace' only while subscription_grace_until > now (epoch seconds)"
    - "isAgentPublishable returns false for 'none' and 'lapsed'"
    - "getStripe initializes the SDK with the Fetch HTTP client and explicit apiVersion (Workers-safe)"
    - "STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID are declared in the CloudflareEnv type and documented in .env.local.example"
  artifacts:
    - path: "src/lib/stripe.ts"
      provides: "getStripe() Workers-safe singleton + stripeCryptoProvider for webhook verification"
      exports: ["getStripe", "stripeCryptoProvider"]
      contains: "createFetchHttpClient"
    - path: "src/lib/subscription.ts"
      provides: "isAgentPublishable() + AGENT_PUBLISHABLE_SQL + getAgentSubscriptionState()"
      exports: ["isAgentPublishable", "AGENT_PUBLISHABLE_SQL", "getAgentSubscriptionState", "SubscriptionStatus", "AgentSubscriptionState"]
      contains: "AGENT_PUBLISHABLE_SQL"
    - path: "src/tests/subscription.test.ts"
      provides: "Behavioral coverage of isAgentPublishable for all 4 statuses + admin bypass + grace expiry"
  key_links:
    - from: "src/lib/subscription.ts"
      to: "D1 agents row"
      via: "getAgentSubscriptionState SELECT subscription_status, subscription_grace_until, is_admin"
      pattern: "subscription_status.*subscription_grace_until.*is_admin"
---

<objective>
Establish the Stripe + subscription foundation that every other Phase 3 plan consumes: the Workers-safe Stripe client factory, the subscription-status helper (with admin bypass and 7-day grace logic), the SQL gate fragment Phase 4 will apply to listing queries, the typed Cloudflare env additions, and the documented secrets. All logic is unit-tested with `node --test`.

Purpose: BILL-04 (subscription status + 7-day grace stored on the D1 agents row and enforced via a reusable SQL gate) and the idempotency/type groundwork for BILL-05. Routes (03-02) and the dashboard widget (03-03) both depend on these modules, so this plan ships first with zero dependencies.

Output: `src/lib/stripe.ts`, `src/lib/subscription.ts`, updated `cloudflare-env.d.ts`, updated `.env.local.example`, and two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-billing/03-CONTEXT.md
@.planning/phases/03-subscription-billing/03-RESEARCH.md

# Schema (already complete — NO migration in this phase)
@db/migrations/0001_initial_schema.sql

# Pattern templates
@src/lib/auth-edge.ts
@src/tests/profile.test.ts
@cloudflare-env.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Stripe SDK and create the Workers-safe Stripe client factory</name>
  <files>package.json, package-lock.json, src/lib/stripe.ts, src/tests/stripe-lib.test.ts</files>
  <behavior>
    - Test: src/lib/stripe.ts source contains `createFetchHttpClient` (Workers requires the Fetch HTTP client — node:https is unavailable).
    - Test: src/lib/stripe.ts pins an explicit `apiVersion: '2026-05-27.dahlia'`.
    - Test: src/lib/stripe.ts exports `getStripe` and `stripeCryptoProvider`.
    - Test: src/lib/stripe.ts does NOT contain the string `runtime = 'edge'` (deviation from Phase 2 — see action).
    - Test: file has a JSDoc `@module` comment and is under 500 lines.
  </behavior>
  <action>
    Install the official Stripe SDK with `npm install stripe` (pins to stripe@22.2.1, API version 2026-05-27.dahlia per RESEARCH Standard Stack). The Package Legitimacy Audit in 03-RESEARCH.md already approved `stripe` (official Stripe org, 13.5M weekly downloads; SUS flag was a false positive from the recency window) — no human-verify checkpoint required.

    Create src/lib/stripe.ts following 03-RESEARCH.md Pattern 1 exactly: a module-level `let _stripe: Stripe | null` singleton and `export function getStripe(secretKey: string): Stripe` that lazily constructs `new Stripe(secretKey, { httpClient: Stripe.createFetchHttpClient(), apiVersion: '2026-05-27.dahlia' })`. Also `export const stripeCryptoProvider = Stripe.createSubtleCryptoProvider()` for webhook verification in 03-02. Include the JSDoc `@module lib/stripe` header.

    DEVIATION FROM PHASE 2 (justified by research): do NOT add `export const runtime = 'edge'` to any Phase 3 file. Phase 2 routes use it, but @opennextjs/cloudflare v1.x does not support the edge runtime declaration and copying it risks build breakage on adapter upgrade (RESEARCH Pitfall 4 / opennext.js.org CITED). Add a short JSDoc note in stripe.ts stating the omission is intentional.

    Create src/tests/stripe-lib.test.ts mirroring the structural-check style of src/tests/profile.test.ts: read src/lib/stripe.ts via fs and assert the behaviors above with node:test + node:assert/strict (no live Stripe import — these are source-structure assertions, deterministic and offline).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "stripe-lib" && npm run typecheck</automated>
  </verify>
  <done>`stripe` is in package.json dependencies; src/lib/stripe.ts exports getStripe + stripeCryptoProvider with createFetchHttpClient and the pinned apiVersion, no edge runtime export; stripe-lib.test.ts passes; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the subscription-status helper, SQL gate, and admin bypass</name>
  <files>src/lib/subscription.ts, src/tests/subscription.test.ts</files>
  <behavior>
    - isAgentPublishable({ is_admin: 1, subscription_status: 'none' }) === true (admin bypass, per CONTEXT Admin decision).
    - isAgentPublishable({ is_admin: 0, subscription_status: 'active' }) === true.
    - isAgentPublishable grace with subscription_grace_until in the future === true.
    - isAgentPublishable grace with subscription_grace_until in the past === false.
    - isAgentPublishable({ subscription_status: 'none' }) === false and 'lapsed' === false.
    - AGENT_PUBLISHABLE_SQL string contains is_admin=1, subscription_status='active', and a grace check against unixepoch().
  </behavior>
  <action>
    Create src/lib/subscription.ts following 03-RESEARCH.md Pattern 6: export type `SubscriptionStatus = 'none' | 'active' | 'grace' | 'lapsed'`; interface `AgentSubscriptionState { subscription_status, subscription_grace_until: number | null, is_admin: number }`; `export function isAgentPublishable(agent): boolean` short-circuiting to true for is_admin===1, true for 'active', true for 'grace' while `Math.floor(Date.now()/1000) < subscription_grace_until`, false otherwise; `export const AGENT_PUBLISHABLE_SQL` (the parenthesized WHERE fragment referencing alias `a`, using `unixepoch()` for the grace comparison — seconds, not ms, per RESEARCH Pitfall 6); and `export async function getAgentSubscriptionState(db, uid)` doing the parameterized `prepare().bind().first()` SELECT. Include `@module lib/subscription`. This helper is DEFINED here and APPLIED to public listing queries in Phase 4 (CONTEXT boundary) — note that in the JSDoc.

    Create src/tests/subscription.test.ts in the project's established style (structural checks that subscription.ts exports the names + inline reference-implementation behavioral tests, mirroring src/tests/profile.test.ts) covering every behavior bullet above, including a grace-expiry boundary case and the admin-bypass case (BILL-04 grace enforcement; admin always publishable per CONTEXT).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "subscription" && npm run typecheck</automated>
  </verify>
  <done>src/lib/subscription.ts exports isAgentPublishable, AGENT_PUBLISHABLE_SQL, getAgentSubscriptionState, and the two types; subscription.test.ts covers all 4 statuses + admin + grace expiry and passes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Declare Stripe secrets in the Cloudflare env type and .env.local.example</name>
  <files>cloudflare-env.d.ts, .env.local.example</files>
  <action>
    Add `STRIPE_SECRET_KEY: string;`, `STRIPE_WEBHOOK_SECRET: string;`, and `STRIPE_PRICE_ID: string;` to the `__BaseEnv_CloudflareEnv` interface in cloudflare-env.d.ts (alongside the existing DB / PERFEX_* entries) so `env.STRIPE_*` is typed in the route handlers built in 03-02. Do NOT regenerate the whole file with `wrangler types` (that would drop the manual edit on the next run unless wrangler.toml secrets are declared) — edit the interface block in place and leave a comment noting these are wrangler secrets, never `[vars]`.

    Append a `# Stripe (Phase 3 — subscription billing)` section to .env.local.example documenting STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, and STRIPE_PRICE_ID with the dashboard source for each (mirror the user_setup frontmatter). These are secrets — example values must be placeholders only (e.g. `sk_test_…`, `whsec_…`, `price_…`); NEVER commit real keys (CLAUDE.md). For local dev these live in `.dev.vars`; document that too.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>cloudflare-env.d.ts types the three STRIPE_* vars; .env.local.example documents all three with placeholder values and dashboard sources; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secrets storage → runtime | Stripe secret key must reach the Worker via wrangler secret, never git or `[vars]` |
| D1 agents row → gate decision | Subscription/grace values read from D1 drive publish authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-IC | Information Disclosure | STRIPE_SECRET_KEY | mitigate | Stored as wrangler secret + `.dev.vars` only; `.env.local.example` carries placeholders; typed in CloudflareEnv but never inlined |
| T-03-GR | Tampering | grace expiry math | mitigate | `Math.floor(Date.now()/1000)` epoch SECONDS compared with `unixepoch()`; ms-vs-seconds bug avoided (RESEARCH Pitfall 6); unit-tested boundary case |
| T-03-AB | Elevation of Privilege | admin bypass logic | mitigate | `is_admin===1` short-circuit unit-tested; no Stripe customer ever created for admins (enforced in 03-02) |
| T-03-SC | Tampering | npm install stripe | mitigate | RESEARCH Package Legitimacy Audit approved `stripe` (official org); no [ASSUMED]/[SUS] blocking checkpoint required |
</threat_model>

<verification>
- `npm test` — subscription.test.ts and stripe-lib.test.ts pass (all 4 statuses, admin bypass, grace expiry, Stripe client structure).
- `npm run typecheck` — clean with STRIPE_* env types added.
- Grep: `src/lib/stripe.ts` contains `createFetchHttpClient` and does NOT contain `runtime = 'edge'`.
- Grep: `src/lib/subscription.ts` exports `AGENT_PUBLISHABLE_SQL`.
</verification>

<success_criteria>
- isAgentPublishable correct for none/active/grace(future)/grace(past)/lapsed + admin bypass.
- getStripe builds a Workers-safe Stripe instance (Fetch HTTP client, pinned apiVersion); stripeCryptoProvider exported for 03-02 webhook verification.
- AGENT_PUBLISHABLE_SQL ready for Phase 4 listing queries.
- Three Stripe secrets typed and documented; no real secrets committed.
</success_criteria>

<output>
Create `.planning/phases/03-subscription-billing/03-01-SUMMARY.md` when done.
</output>
