/**
 * OpenNext Cloudflare Configuration
 *
 * Configures the @opennextjs/cloudflare adapter for Cloudflare Workers deployment.
 * No options needed for Phase 1 — default config handles routing, assets, and bindings.
 *
 * @module open-next.config
 */
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
